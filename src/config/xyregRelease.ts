/**
 * Current XYREG release metadata.
 * This is a hardcoded fallback — in production, the latest published release
 * is fetched from the xyreg_releases table.
 */
export const CURRENT_XYREG_RELEASE = {
  version: '2.4.1',
  date: '2025-03-15',
} as const;
